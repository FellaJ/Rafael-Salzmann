
/* trigger when page is ready */

$(document).ready(function(){
	$('.single-item').slick({
		infinite: true,
		dots: false,
		arrows: false,
		autoplay: true,
		autoplaySpeed: 3500,
		fade: true,
		fadeSpeed: 900
	});
});
